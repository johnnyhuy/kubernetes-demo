apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
spec:
  ports:
    - port: 3000
      protocol: TCP
      name: http
  selector:
    kubernetes.io/name: todo-frontend
---

apiVersion: v1
kind: Service
metadata:
  name: todo-backend
spec:
  ports:
    - port: 3001
      protocol: TCP
      name: http
  selector:
    kubernetes.io/name: todo-backend
